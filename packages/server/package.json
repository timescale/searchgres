{
  "name": "@searchgres/server",
  "version": "0.0.0",
  "private": true,
  "description": "Bun JSON-RPC server for one searchgres index.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "bun": ">=1.4.0"
  },
  "scripts": {
    "bundle:tokenizer-worker": "../../bun scripts/bundle-tokenizer-worker.ts",
    "build": "../../bun run bundle:tokenizer-worker && tsc -p tsconfig.build.json && cp src/tokenizer.worker.generated.cjs dist/tokenizer.worker.generated.cjs",
    "clean": "rm -rf dist tsconfig.build.tsbuildinfo src/tokenizer.worker.generated.cjs",
    "typecheck": "../../bun run bundle:tokenizer-worker && tsc -p tsconfig.json",
    "test:unit": "../../bun run bundle:tokenizer-worker && find src -name '*.test.ts' ! -name '*.integration.test.ts' -print0 | xargs -0 ../../bun test",
    "test:db": "test -x ../../dist/searchgres-server && test -x ../../dist/searchgres && test -x ../../dist/searchgres-mcp || { echo \"binaries are missing; run ./bun run compile\" >&2; exit 1; }; ../../bun test ./src/*.integration.test.ts",
    "compile": "mkdir -p ../../dist && ../../bun build ./src/bin.ts --compile --outfile ../../dist/searchgres-server && ../../bun ../../scripts/macos-sign.ts ../../dist/searchgres-server",
    "compile:all": "../../bun ../../scripts/compile-targets.ts searchgres-server ./src/bin.ts"
  },
  "dependencies": {
    "@ai-sdk/openai": "^4.0.52",
    "@huggingface/tokenizers": "^0.1.3",
    "@searchgres/protocol": "workspace:*",
    "gpt-tokenizer": "^3.4.0",
    "postgres": "^3.4.9",
    "searchgres": "workspace:*",
    "@clack/prompts": "^0.11.0",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "1.29.0"
  }
}
