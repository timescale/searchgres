{
  "name": "@searchgres/protocol",
  "version": "0.0.0",
  "private": true,
  "description": "Shared JSON-RPC wire schemas for searchgres surfaces.",
  "license": "Apache-2.0",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "clean": "rm -rf dist tsconfig.build.tsbuildinfo",
    "typecheck": "tsc -p tsconfig.json",
    "test:unit": "node --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
